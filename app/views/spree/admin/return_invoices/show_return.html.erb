<div class="container-fluid">
<h5>Прибуткова накладна</h5>
<table class="table table-bordered">
  <thead>
      <th scope="col">Номер</th>
      <th scope="col">Дата</th>
      <th scope="col">Зменшення у доларах</th>
  </thead>
  <tbody>
    <tr>
      <td><%=@return_invoice.name%></td>
      <td><%=@return_invoice.date.strftime("%d-%m-%Y")%></td>
      <td><%=@return_invoice.income_usd%></td>
    </tr>
  </tbody>
</table>
<h6>Товари</h6>
  <table class="table table-bordered">
    <thead>

         <th scope="col">Назва</th>
          <th scope="col">Ціна</th>
          <th scope="col">Кількість</th>

        </thead>
        <tbody>
          <%@return_invoice.invoice_items.each do |item|%>
          <tr>
              <td><%=item.name%></td>
              <td><%=item.price%></td>
              <td><%=item.quantity%></td>
          </tr>
              <%end%>

    </tbody>
</table>
<hr>
<div class="row">
  <div class="col-6">
<%=link_to "Повернутися", admin_show_mutual_settlement_path(id: @return_invoice.mutual_settlement.id)%>
</div>
<div class="col-6">
<%=link_to "Видалити", admin_delete_return_invoice_path(id: @return_invoice.id), method: :delete%>
</div>
</div>
</div>
