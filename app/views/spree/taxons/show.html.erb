<div class="taxon">
  <div class="container">
    <%= spree_breadcrumbs(@taxon, @taxon.id) %>
    <%= render 'header', taxon: @taxon %>
  </div>
  <%= render 'subcategories', taxon: @taxon, id: @taxon.id %>
  <div data-hook="taxon_products">
    <%= render template: 'spree/products/index', id: @taxon.id %>
  </div>
</div>
